# WordPress Custom Theme

Repository นี้จัดเก็บ Source Code สำหรับธีม WordPress ที่ปรับแต่งพิเศษ (Custom Theme) โดยพัฒนาในรูปแบบ **Child Theme** ที่อ้างอิงจากธีมหลัก **GeneratePress**

## 🚀 ภาพรวมโปรเจกต์ (Project Overview)

ธีมนี้ถูกออกแบบมาเพื่อรองรับฟังก์ชันการทำงานเฉพาะทางสำหรับธุรกิจคลินิกความงาม โดยมีการขยายความสามารถจาก WordPress ปกติ ดังนี้:

*   **Custom Post Types & Metaboxes:**
    *   **Doctors:** ระบบจัดการข้อมูลแพทย์ (ตารางเวร, ข้อมูลส่วนตัว)
    *   **Branches:** ระบบจัดการข้อมูลสาขา
    *   **Case Reviews:** ระบบรีวิวเคสลูกค้า
    *   **Promotions & Banners:** ระบบจัดการโปรโมชั่นและแบนเนอร์
*   **Admin UI Customization:**  ปรับแต่งหน้าตาหลังบ้าน (WP Admin) ให้ใช้งานง่ายและตรงกับ Branding
*   **Performance Optimization:** การจัดการ CSS/JS แยกตามส่วนการใช้งาน

## 📂 โครงสร้างไฟล์ (File Structure)

โครงสร้างหลักของธีมในโฟลเดอร์ `generatepress_child`:

```text
generatepress_child/
├── assets/
│   ├── css/              # Stylesheets สำหรับ Frontend และ Admin
│   │   ├── admin/        # CSS เฉพาะสำหรับหน้า Admin
│   │   └── ...
│   ├── fonts/            # Web Fonts (Gotham Font Family)
│   ├── js/               # JavaScript files
│   └── scss/             # SCSS Source files
├── inc/                  # Core Functions & Logic
│   ├── metabox-*.php     # การตั้งค่า Metabox สำหรับ Post Type ต่างๆ
│   └── options-*.php     # การตั้งค่า Options เสริม
├── functions.php         # Main Theme Functions
├── style.css             # Main Stylesheet & Theme Header
└── single-page_*.php     # Templates เฉพาะสำหรับ Post Type ต่างๆ
```

## 🛠 Tech Stack & Requirements

*   **Core:** WordPress (Latest Version recommended)
*   **Parent Theme:** [GeneratePress](https://generatepress.com/)
*   **Languages:** PHP, HTML5, SCSS/CSS3, JavaScript (jQuery/Vanilla)
*   **Dependencies:**
    *   ต้องติดตั้งธีม GeneratePress (Parent) ก่อนใช้งาน

## 📦 การติดตั้ง (Installation)

1.  ดาวน์โหลดหรือ Clone repository นี้
2.  นำโฟลเดอร์ `generatepress_child` ไปวางไว้ที่ `wp-content/themes/` บน Server
3.  เข้าไปที่หน้าจัดการ WordPress (Admin) > **Appearance** > **Themes**
4.  กด **Activate** ธีม "GeneratePress Child"

## 📝 หมายเหตุสำหรับการพัฒนา (Development Notes)

*   **Styles:** ควรแก้ไขไฟล์ `.scss` ใน `assets/scss/` และ Compile เป็น `.css` แทนการแก้ไขไฟล์ `.css` โดยตรง เพื่อให้โค้ดเป็นระเบียบและดูแลรักษาง่าย
*   **Functions:** ฟังก์ชันหลักถูกแยกเก็บไว้ในโฟลเดอร์ `inc/` เพื่อลดความซับซ้อนของไฟล์ `functions.php`

---
*Last Updated: 2026*
